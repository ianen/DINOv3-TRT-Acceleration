# V1.0.3 First G7 Utilization Data Point — r224 b1 BF16 prefer

**Date**: 2026-05-02
**Run ID**: `v103_g7_first_bf16-prefer_r224_b1_n1-2-4_20260502-211434`
**Status**: V1.0.3 G7 acceptance gate baseline established (single-resolution / single-batch); per-N stream breakdown pending (orchestrator currently aggregates across full benchmark).

## Setup

- Hardware: RTX 5080 sm_120 (Blackwell), driver 591.86, 16303 MiB VRAM
- Engine: `Code/Artifacts/engines/dinov3_vitl16_4out.bf16.prefer.engine` (V1.0.1 r224 baseline)
- Tool: `Code/scripts/run_v103_benchmark_with_utilization.ps1` (V1.0.3 G7 orchestrator) + `benchmark_multi_stream.py` (V1.0.2 ADR-015 driver)
- Config: r=224, b=1, num-streams=1,2,4, warmup=50, iters=2000
- Wallclock: 34.27 s (covered all three stream counts)
- Sampling: nvidia-smi `-lms 100` cadence (~10 Hz), 263 timeline samples after 1 s warmup skip

## Throughput (V1.0.3 G1 r224 b1)

| N (streams) | aggregate qps | speedup vs N=1 | p50 lat / worker (ms) |
|---|---|---|---|
| 1 | **344.59** | 1.000 | 2.89 |
| 2 | **516.28** | **1.498** | 3.83 |
| 4 | 504.73 | 1.465 | 7.73 |

**Finding**: Python ThreadPoolExecutor multi-stream tops out at ~1.5× single-stream (matches V1.0.2 ADR-015 finding 1.513×). N=4 *regresses* slightly vs N=2 — GIL contention + per-worker engine duplication cost dominates.

**G1 target = ≥ 800 qps NOT MET** by Python multi-stream alone. Confirms V1.0.3 plan thesis: **C++ multi-context pool (ADR-020) and/or Triton dynamic batching (ADR-019) required**.

## GPU Utilization (V1.0.3 G7, cross-N aggregate)

This single row averages utilization across the full 26.3 s sampled window (covering N=1, N=2, N=4 sequential phases). Per-N breakdown requires running each stream count as a separate benchmark — pending orchestrator refactor.

| Metric | Value | G7 gate | Verdict |
|---|---|---|---|
| `sm_pct_mean` | 87.4% | r518 b8 ≥ 95% (saturation) | r224 b1 below SM saturation gate; expected for compute-bound but not memory-bound regime |
| `sm_pct_p50` | 92.0% | — | majority of samples high |
| `sm_pct_p95` | 99.0% | — | brief saturation observed during compute spikes |
| `sm_pct_max` | 99.0% | — | physical ceiling reached momentarily |
| `hbm_pct_mean` | 29.0% | b≥16 ≥ 90% | r224 b1 is compute-bound, HBM gate only checked at b≥16 — confirms regime classification |
| `tensor_core_pct` | NaN (ncu not invoked) | TC ∈ [65, 75]% | next run will add `-NcuTargetExe` to capture HMMA pipe utilization |
| `avg_power_w` | 232.6 W | < 300 W TDP | ✓ comfortable headroom |
| `peak_power_w` | 259.5 W | < 300 W | ✓ no thermal-throttle risk |
| `peak_temperature_c` | 65.0 °C | — | ✓ no throttle |

## Tools Validation

The V1.0.3 G7 toolchain validated end-to-end on Windows:
- `utilization_monitor.ps1` produces nvidia-smi 100 ms cadence timeline.csv with correct schema
- `aggregate_utilization.py` computes mean / p50 / p95 / max / power statistics; appends to V1.0.3 §10.3 benchmark CSV
- `run_v103_benchmark_with_utilization.ps1` couples nvidia-smi start/stop to benchmark wallclock (fixes initial bug where 60 s monitor diluted SM% with idle samples → reported 0%)
- Output: `Code/Artifacts/reports/v103_throughput_matrix.csv` row appended with G7 columns `sm_pct, tensor_core_pct, hbm_pct, avg_power_w` (matches plan §10.3 schema)

## Implications for V1.0.3 ADR Sequence

1. **ADR-020 (C++ multi-context pool) now empirically justified** — Python path 1.5× ceiling proven, can't reach G1 800 qps. C++ pool with shared `ICudaEngine` + N `IExecutionContext` over CUDA streams is the lowest-risk path to G1.
2. **ADR-019 (Triton)** — currently parked due to Docker/WSL2 user-blocker on Windows host. Once unblocked it would augment with dynamic batching (request aggregation in 1 ms windows can lift batch utilization on top of multi-context).
3. **Per-N utilization breakdown** — needed to verify G7 acceptance "r224 b1 N=2 SM% − N=1 SM% ≥ 15 pp". Orchestrator refactor: drive 3 separate runs (one per stream count) instead of one combined.
4. **Tensor Core measurement** — first run skipped ncu invocation. Next run will pass `-NcuTargetExe python -NcuTargetArgs "scripts\benchmark_multi_stream.py ..."` to capture HMMA pipe activity; this is the V1.3 QAT door (Tensor Core ceiling in BF16 dense path → quantify the gap that INT8/FP8/2:4 sparse via QAT could close).
5. **r518 b8 single-stream G7 saturation test** — needs separate run with `-Engine ...r518.bf16.prefer.b8.engine -BatchSize 8 -ImageSize 518 -NumStreams 1` to verify SM ≥ 95% claim (V1.0.2 ADR-015 indirectly inferred this from N=2 → 1.023× near-zero gain; G7 will *quantify* it).

## Next Actions

- [ ] Refactor `run_v103_benchmark_with_utilization.ps1` to do one run per N stream count → 3 rows per (resolution, batch) instead of 1 combined row
- [ ] Run r336 b8 + r518 b8 single-stream variants to fill V1.0.3 §6 acceptance gate G7 thresholds
- [ ] Add ncu sampling to capture Tensor Core utilization; this enables the V1.3 QAT motivation argument for the paper
- [ ] Start ADR-020 C++ pool implementation (now empirically justified) — Week 3-4 work pulled forward to Week 1 because Triton blocked
