# V1.0.4 Implementation Status Snapshot — 2026-05-07

V1.0.4（r=512 印花布数据集生产环境推理 benchmark）实施期 mid-status。Day 1 工作完成除 Windows-dependent steps 外的全部内容；剩余实测待 user 重启 RTX 5080 工作站后继续。

## TL;DR

- **Code 100% ship**：6 commits to `main` 涵盖 ADR-024 / 025 / 026 / 027 + 2 launchers + 5 ADR docs
- **G1 数据集准备 ✅ PASS**：144 张 r=512 + manifest（local Mac 完成，无需 Windows）
- **G2-G6 awaiting Windows SSH**：cpolar tunnel `8.tcp.vip.cpolar.cn:13495` connection refused（user 确认机器临时关机）
- 解封后**单命令完整重启**：`run_v104_benchmark_windows.ps1` 全 sweep + 报告 PDF 自动产出

## V1.0.4 Goal Progress

| # | Goal | Target | 实测 / 状态 | 验收 |
|---|---|---|---|---|
| **G1** | 数据集 1024 → 512 离线 resize | 144 张 + manifest | **✅ PASS** 144/144 / 9.9 MB / 0 errors | commit `44c6299` |
| **G2** | r=512 FP32 + BF16 prefer engine build + cos > 0.999 | 2 engines | ⏸ awaiting SSH | code ready (`build_v104_engines_windows.ps1`) |
| **G3** | 6 段独立 latency p50 | disk/decode/preprocess/H2D/enqueueV3/D2H | ⏸ awaiting SSH | code ready (Python + C++) |
| **G4** | Pareto 8 必选数据点 | 4 batch × 2 必选精度 | ⏸ awaiting SSH | launcher ready (`run_v104_benchmark_windows.ps1`) |
| **G5** | Python ↔ C++ diff ≤ 10% + cos = 1.0 | parity | ⏸ awaiting data | code ready (mirror schema) |
| **G6** | 报告 + PDF | 7-10 页 A4 | ⏸ awaiting data | skeleton + ADR-028 ready |

**Score: 1/6 ✅ PASS + 5/6 ⏸ user-blocker (SSH)**

## 5 ADR 状态

| ADR | 主题 | Status |
|---|---|---|
| **ADR-024** | Dataset 1024 → 512 resize | **Implemented** ✅ (commit `44c6299`) |
| **ADR-025** | r=512 ONNX export + engine build | **Code Implemented · Awaiting SSH** (commit `591672d` + `4f4ff6c`) |
| **ADR-026** | Python production_benchmark.py | **Code Implemented · Awaiting SSH** (commit `3b0594b`) |
| **ADR-027** | C++ production_benchmark.cpp | **Code Implemented · Awaiting SSH** (commit `4f4ff6c`) |
| **ADR-028** | Report synthesis | **Skeleton Drafted · Awaiting Data** |

## 6 Commits 详细索引

| Commit | Subject | Files |
|---|---|---|
| `44c6299` | ADR-024 dataset r=512 resize + 144 张 + manifest | `prepare_r512_dataset.py` (+296) |
| `591672d` | export_official_dinov3_onnx.py 加 --image-size 参数 | `export_official_dinov3_onnx.py` (+12) |
| `3b0594b` | ADR-026 production_benchmark.py 6 段计时 | `production_benchmark.py` (+489) |
| `4f4ff6c` | ADR-027 production_benchmark.cpp + stb_image vendor + launchers | `production_benchmark.cpp` / `stb_image.h` / CMakeLists / 2× ps1 (+9000+) |
| (next) | 5 ADR docs + V1.0.4 status + INDEX update + report skeleton | (~10 files) |

## 用户决策点

**单一 user-blocker**：RTX 5080 Windows 工作站需要重启（cpolar SSH tunnel 已 confirm refused）。

| 选项 | 行动 | 解锁 |
|---|---|---|
| **A** （推荐）| user 给 Windows 上电 + 启动 cpolar service | 完整 V1.0.4 sweep + 报告自动产出 |
| **B** | 切到本地 IP `192.168.1.233`（如同网段） | 同 A |
| **C** | 暂停 V1.0.4 实测，agent 继续 V1.0.5 候选 / paper 工作 | 只能写 / 整理文档 |

## 解封后一键续跑命令

```bash
# Step 1: sync 最新 code 到 Windows
python Code/scripts/sync_remote_windows_repo.py

# Step 2: build engines (FP32 + BF16, ~5 min)
ssh windows-pc 'powershell -ExecutionPolicy Bypass -File Code\scripts\build_v104_engines_windows.ps1'

# Step 3: build cpp benchmark binary (~3 min)
ssh windows-pc 'powershell -ExecutionPolicy Bypass -File Code\scripts\build_cpp_trt_inspector_windows.ps1'

# Step 4: cos verify r=512 BF16 vs FP32 单图（必须 G2 PASS 才继续）
ssh windows-pc 'cd /d D:\WorkPlace\ZMP\DINOv3-TRT-Acceleration && .venv\Scripts\python.exe Code\scripts\evaluate_engine_pair_on_images.py --reference-engine Artifacts\engines\dinov3_vitl16_4out.r512.fp32.engine --candidate-engine Artifacts\engines\dinov3_vitl16_4out.r512.bf16.prefer.engine --image-root Artifacts\datasets\good_r512 --image-size 512 --max-images 1'

# Step 5: full sweep (~30 min for 8 必选数据点 × 2 语言 = 16 JSON)
ssh windows-pc 'powershell -ExecutionPolicy Bypass -File Code\scripts\run_v104_benchmark_windows.ps1'

# Step 6: pull reports + 报告 markdown 撰写 + PDF
python Code/scripts/sync_remote_windows_repo.py --pull-reports
# (agent will draft r512_production_benchmark_V1.0.4.md from CSV)
~/.claude/skills/md2pdf/md2pdf.sh Wiki/2-技术报告/r512_production_benchmark_V1.0.4.md

# Step 7: commit + push
git add Wiki/2-技术报告/r512_production_benchmark_V1.0.4.{md,pdf} \
        Code/Artifacts/reports/v104_production_benchmark_matrix.csv
git commit -m "feat(v104): r=512 production benchmark report + 16-row CSV ship"
git push origin main
```

## 心跳状态

`goal-v104-print-fabric-r512-2122` (id `f9d5eada`) 30min cadence active。下次心跳 fire 时若仍 SSH 不通，agent 自动报告 + 继续相邻文档工作（已无更多相邻可做项 — 当前已饱和）。

**建议 user**：上电 5080 后心跳就会自动检测并继续；如果不打算今天上电，可 `paseo schedule delete f9d5eada` 关闭心跳，未来手动 `/goal "完成 V1.0.4 ..."` 重启。

## 一句话总结

V1.0.4 Day 1 在不依赖 Windows 的情况下**全部代码 ship**（6 commits + 5 ADR docs + status snapshot + 报告骨架）；剩余 G2-G6 待 user 上电 RTX 5080 工作站后单命令一键续跑（预计 30-45 min 完成实测 + 报告产出）。
